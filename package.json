{
  "name": "gyazo-paste",
  "version": "0.0.0",
  "dependencies": {
    "whatwg-fetch": "^0.9.0"
  },
  "devDependencies": {
    "browserify": "^9.0.8",
    "domready": "^1.0.7",
    "standard": "^3.7.0"
  },
  "scripts": {
    "build": "browserify src/content.js > dist/content.js",
    "test": "standard src/**/*.js"
  },
  "engines": {
    "node": ">=0.10.0"
  }
}
